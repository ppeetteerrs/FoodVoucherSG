import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ViewBatch } from './view-batch';

@NgModule({
  declarations: [
    ViewBatch,
  ],
  imports: [
    IonicPageModule.forChild(ViewBatch),
  ],
})
export class ViewBatchModule {}
